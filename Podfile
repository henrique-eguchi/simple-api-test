# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'simple-api-test' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for simple-api-test

  target 'simple-api-testTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick', '~> 2.2.0'
    pod 'Nimble', '~> 8.0.0'
    pod 'Nimble-Snapshots'
  end

  target 'simple-api-testUITests' do
    # Pods for testing
  end

end
